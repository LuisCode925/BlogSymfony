{% extends 'base.html.twig' %}

{% block title %}{{ post[0].title }}{% endblock %}

{% block body %}
    {{ parent() }}

    {#% set LikesDeEstePost = post.likes | split(',') %#}
    <div class="container bg-white rounded my-3 p-4">

        <div class="d-flex bd-highlight">
            <div class="p-2 flex-grow-1 bd-highlight">
                <h2>{{ post[0].title }}</h2>
                {#<div class="my-3" style="color:#3282b8;" onclick="MeGusta({{ post.id }})">
                    <i class="fa fa-thumbs-up"></i>
                    <strong>{#% if app.user.id in  LikesDeEstePost %}Like{% else %}Dislike{% endif %}</strong>
                </div>#}
                <div style="color:#c3f584;">
                    <h4>Fecha: {{ post[0].created_at | date('d-m-Y') }} a las {{ post[0].created_at | date('H:i:s') }}</h4>
                    <h4>Autor: {{ post[0].names ~ " " ~ post[0].last_names }}</h4>
                </div>
            </div>
            <div class="p-2 bd-highlight">
                <div class="align-self-center">
                    <a href="{{ asset('uploads/thumb/' ~ post[0].thumb )}}" data-lightbox="{{ post[0].thumb }}" data-title="{{ post[0].title }}">
                        <img class="rounded" data-lightbox="{{ post[0].thumb }}" src="{{ asset('uploads/thumb/' ~ post[0].thumb )}}" width="350px">
                    </a>
                </div>
            </div>
        </div>
        <hr>
        <div class="text-justify my-3">
            {{ post[0].content }}
        </div>
        <hr>

    </div>
    

    {#<div class="container p-3">
        {{ form(form) }}
        <h3>Comentarios</h3>
        {% for comentario in comentarios %}
            <div class="rounded bg-white my-2 p-3 border">
                <h5>{{ comentario.nombre }} Dijo:</h5>
                <div class="w-100">{{ comentario.comentario }}</div>
            </div>
        {% endfor %}
    </div>

    <div class="d-flex justify-content-center">
        <div class="navigation">
            {{ knp_pagination_render(comentarios) }}
        </div>
    </div>#}
    
{% endblock %}
